include adan.fs;
include adan.io;

program::string parseFile(f::string) {
    return (string)f;
}

program::int main() {
    f::string = read_file("./examples/my-program.adn");
    bin::string = parseFile(f);

    println(f);
    println(bin);

    write_file("./compiled/proprogram", bin);

    return 0;
}