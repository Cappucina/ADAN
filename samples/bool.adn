import "adan/io";

set age: i32 = 18;
set isAdult: bool = age >= 18;

fun main(): i32 {
    if isAdult {
        println("Is over the age of 18.");
    } else {
        println("Is not over 18!");
    }

    return 0;
}